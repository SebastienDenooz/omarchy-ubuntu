#!/bin/bash
# Ubuntu: true when every package (Arch names translated) is installed.
for p in $(omarchy-ubuntu-pkgmap "$@"); do
  [[ $p == SKIP ]] && exit 1
  dpkg-query -W -f='${Status}' "$p" 2>/dev/null | grep -q 'install ok installed' || exit 1
done
exit 0
