#!/bin/bash
# Recrée les liens /usr/local/bin/omarchy-* vers /usr/share/omarchy/bin (à rejouer après une mise à jour).
for f in /usr/share/omarchy/bin/omarchy*; do sudo ln -sfn "$f" "/usr/local/bin/$(basename "$f")"; done
